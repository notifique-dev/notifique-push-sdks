/**
 * Hosted CDN script for Notifique Web Push.
 * Source of truth for the typed SDK: npm package `@notifique/push` (monorepo notifique-push-sdks/packages/web).
 * Sync the IIFE build (`dist/index.global.js`) here on each release, or keep this legacy loader until CDN cutover.
 *
 * Usage:
 *   <script src="https://api.notifique.dev/v1/push/scripts/notifique-push.js"></script>
 *   <script>NotifiquePush.init({ appId: 'seu-app-id', swPath: '/sw.js' });</script>
 */
